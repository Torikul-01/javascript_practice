function cutHalf(number){
const half = number / 2;
return half;
} 
const result = cutHalf(10);
console.log(result);

function sum(num1, num2) {
    const total = num1 + num2;
    return total;
}
const sumResult = sum(5, 3);
console.log(sumResult);

const total2=add(result, sumResult);
console.log(total2);
